[build]
builder = "nixpacks"

[deploy]
startCommand = "python manage.py migrate --noinput && python -m gunicorn recovery_center.wsgi --log-file - --bind 0.0.0.0:$PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
